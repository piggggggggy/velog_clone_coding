import React from "react";

const Detail = (props) => {

    return (
        <React.Fragment>
            DetailBody
            NameCard
            CommentBody
        </React.Fragment>
    );
}

export default Detail;