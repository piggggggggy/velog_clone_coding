import React from "react";

const Posting = (props) => {

    return (
        <React.Fragment>
            NameCard
            PostingBody
        </React.Fragment>
    );
}

export default Posting;