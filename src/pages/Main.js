import React from "react";
import MainBody from '../components/MainBody';
const Main = (props) => {

    return (
        <React.Fragment>
            <MainBody/>
        </React.Fragment>
    );
}

export default Main;