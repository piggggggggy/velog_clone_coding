import React from "react";

const Main = (props) => {

    return (
        <React.Fragment>
            MainCard
            MainBody
            Modal
        </React.Fragment>
    );
}

export default Main;